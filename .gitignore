__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
!.env.example
*.pem
*.p8
private_key*
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.DS_Store
uv.lock.bak
/outputs/

# Service-account keys. The repo is public, and a key that reaches it is
# compromised the moment it is pushed -- rotating is the only remedy, so the
# rule is belt and braces rather than one pattern.
*service-account*.json
*service_account*.json
*credentials*.json
gcp-*.json
*.key.json

# Working notes, not for the repo
NOTES.local.md
